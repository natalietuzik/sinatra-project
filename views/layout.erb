
<html>
  <head>
    <title>Let's Find a Recipe!</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
      body {
        font-family: 'Roboto', sans-serif;
        margin: 0;
        padding: 0;
        background-color: #E6F7FF; /* Soft blue background */
        color: #222; /* Dark text for contrast */
      }

      .container {
        width: 80%;
        margin: 0 auto;
      }

      .recipe {
        background-color: #FFFFFF;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        padding: 20px;
        margin-top: 20px;
        transition: transform 0.3s;
      }

      .recipe:hover {
        transform: translateY(-5px);
      }

      .recipe img {
        max-width: 100%;
        height: auto;
        border-radius: 10px;
        margin-bottom: 20px;
      }

      .recipe h2 {
        color: #007BFF; /* Bright blue for headings */
        margin-bottom: 10px;
      }

      .recipe ul {
        list-style-type: disc;
        padding-inline-start: 20px;
      }

      .recipe ul li {
        margin-bottom: 5px;
        color: #555; /* Lighter shade for list items */
      }

      .recipe h3 {
        color: #0056b3; /* Darker blue for subheadings */
        margin-top: 15px;
      }

      ol {
        padding-inline-start: 20px;
        color: #555; /* Lighter shade for ordered list items */
      }

      @media only screen and (max-width: 768px) {
        .container {
          width: 90%;
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
        <%= yield %>
    </div>
  </body>
</html>
